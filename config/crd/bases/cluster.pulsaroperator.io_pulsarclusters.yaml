---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: pulsarclusters.cluster.pulsaroperator.io
spec:
  group: cluster.pulsaroperator.io
  names:
    kind: PulsarCluster
    listKind: PulsarClusterList
    plural: pulsarclusters
    singular: pulsarcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.pulsarVersion
      name: Version
      type: string
    - jsonPath: .status.brokerPhase
      name: Broker
      type: string
    - jsonPath: .status.bookKeeperPhase
      name: BookKeeper
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PulsarCluster is the Schema for the pulsarclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PulsarCluster
            properties:
              autoRecovery:
                description: autoRecovery configures the autorecovery component.
                properties:
                  config:
                    additionalProperties:
                      type: string
                    description: config sets bookkeeper.conf key/value overrides layered
                      on top of operator defaults.
                    type: object
                  image:
                    description: |-
                      image overrides the autorecovery container image. Falls back to
                      PulsarCluster.spec.image when unset.
                    type: string
                  mode:
                    default: embedded
                    description: |-
                      mode selects whether autorecovery runs embedded in bookie pods or as its
                      own dedicated deployment. The operator always deploys it as a standalone
                      StatefulSet; "embedded" additionally colocates the auditor process
                      configuration with bookies for smaller clusters.
                    enum:
                    - embedded
                    - dedicated
                    type: string
                  replicas:
                    default: 1
                    description: replicas is the number of autorecovery pods.
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              bookKeeper:
                description: bookKeeper configures the bookie component.
                properties:
                  autoRackConfig:
                    description: autoRackConfig configures the bookie rack-awareness
                      sync daemon.
                    properties:
                      enabled:
                        default: false
                        description: enabled turns on the rack-awareness sync daemon.
                        type: boolean
                      periodSeconds:
                        default: 60
                        description: periodSeconds is the interval between rack-metadata
                          sync passes.
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                  autoscaler:
                    description: autoscaler configures the disk-watermark-driven autoscaler.
                    properties:
                      diskUsageToleranceHwm:
                        default: 92
                        description: |-
                          diskUsageToleranceHwm is the high watermark; a writable bookie at or above
                          this disk usage triggers a pre-emptive scale-up. Expressed as a
                          whole-number percent in the range 0-100 (e.g. 92 means 92% disk used);
                          autoscaler controllers divide by 100.
                        format: int32
                        maximum: 100
                        minimum: 0
                        type: integer
                      diskUsageToleranceLwm:
                        default: 75
                        description: |-
                          diskUsageToleranceLwm is the low watermark; every writable bookie must be
                          below this, with zero under-replicated ledgers cluster-wide, before a
                          scale-down is considered. Expressed as a whole-number percent in the range
                          0-100 (e.g. 75 means 75% disk used); autoscaler controllers divide by 100.
                        format: int32
                        maximum: 100
                        minimum: 0
                        type: integer
                      enabled:
                        default: false
                        description: enabled turns on the bookie disk-watermark autoscaler
                          (scale-up).
                        type: boolean
                      minWritableBookies:
                        description: |-
                          minWritableBookies is the minimum number of writable bookies to maintain.
                          Must be >= the largest configured ensembleSize.
                        format: int32
                        minimum: 1
                        type: integer
                      periodSeconds:
                        default: 10
                        description: periodSeconds is the interval between autoscaler
                          evaluations.
                        format: int32
                        minimum: 1
                        type: integer
                      scaleDownBy:
                        default: 1
                        description: scaleDownBy is the number of bookies to remove
                          on each scale-down.
                        format: int32
                        minimum: 1
                        type: integer
                      scaleDownEnabled:
                        default: false
                        description: |-
                          scaleDownEnabled gates the guarded bookie decommission state machine.
                          Kept OFF by default; enable only after validating the safe-decommission
                          workflow against your metadata store.
                        type: boolean
                      scaleUpBy:
                        default: 1
                        description: scaleUpBy is the number of bookies to add on
                          each scale-up.
                        format: int32
                        minimum: 1
                        type: integer
                      scaleUpMaxLimit:
                        description: |-
                          scaleUpMaxLimit is the maximum number of bookies. The autoscaler never
                          scales above this.
                        format: int32
                        minimum: 1
                        type: integer
                      stabilizationWindowSeconds:
                        default: 300
                        description: |-
                          stabilizationWindowSeconds gates scaling decisions until all bookie pods
                          have been continuously Ready for this long, preventing flapping.
                        format: int32
                        minimum: 0
                        type: integer
                    type: object
                    x-kubernetes-validations:
                    - message: Lwm must be below Hwm
                      rule: self.diskUsageToleranceLwm < self.diskUsageToleranceHwm
                  config:
                    additionalProperties:
                      type: string
                    description: |-
                      config sets bookkeeper.conf key/value overrides layered on top of operator
                      defaults. Structural/wiring keys the operator owns —
                      journalDirectories, ledgerDirectories, indexDirectories, bookiePort,
                      httpServerEnabled, and httpServerPort — are re-asserted by the operator to
                      keep the rendered config in sync with the generated Service, probes, and
                      volume mounts, so overrides of those specific keys are ignored. Every
                      other key is applied as given.
                    type: object
                  ensemble:
                    description: ensemble configures the default ledger ensemble/write/ack
                      quorum sizes.
                    properties:
                      ackQuorum:
                        default: 2
                        description: ackQuorum is the number of acks required before
                          an entry is confirmed.
                        format: int32
                        minimum: 1
                        type: integer
                      ensembleSize:
                        default: 3
                        description: ensembleSize is the number of bookies each ledger
                          is striped across.
                        format: int32
                        minimum: 1
                        type: integer
                      writeQuorum:
                        default: 2
                        description: writeQuorum is the number of bookies each entry
                          is written to.
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                    x-kubernetes-validations:
                    - message: ackQuorum <= writeQuorum <= ensembleSize
                      rule: self.ackQuorum <= self.writeQuorum && self.writeQuorum
                        <= self.ensembleSize
                  image:
                    description: |-
                      image overrides the bookie container image. Falls back to
                      PulsarCluster.spec.image when unset.
                    type: string
                  podManagementPolicy:
                    default: Parallel
                    description: |-
                      podManagementPolicy is the StatefulSet pod management policy. Immutable
                      once set.
                    enum:
                    - OrderedReady
                    - Parallel
                    type: string
                    x-kubernetes-validations:
                    - message: podManagementPolicy is immutable
                      rule: self == oldSelf
                  replicas:
                    description: replicas is the number of bookie pods.
                    format: int32
                    minimum: 0
                    type: integer
                  volumes:
                    description: volumes configures the journal/ledgers/index disk-role
                      volumes.
                    properties:
                      index:
                        description: index is the ledger-index volume.
                        properties:
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            description: size is the requested storage capacity.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClassName:
                            description: |-
                              storageClassName is the StorageClass used to provision the volume's PVC.
                              Falls back to PulsarCluster.spec.global.storageClassName when unset.
                            type: string
                        type: object
                      journal:
                        description: journal is the write-ahead-log volume.
                        properties:
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            description: size is the requested storage capacity.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClassName:
                            description: |-
                              storageClassName is the StorageClass used to provision the volume's PVC.
                              Falls back to PulsarCluster.spec.global.storageClassName when unset.
                            type: string
                        type: object
                      ledgers:
                        description: ledgers is the ledger-storage volume.
                        properties:
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            description: size is the requested storage capacity.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClassName:
                            description: |-
                              storageClassName is the StorageClass used to provision the volume's PVC.
                              Falls back to PulsarCluster.spec.global.storageClassName when unset.
                            type: string
                        type: object
                    type: object
                type: object
                x-kubernetes-validations:
                - message: ensembleSize cannot exceed replicas
                  rule: '!has(self.ensemble) || !has(self.ensemble.ensembleSize) ||
                    !has(self.replicas) || self.ensemble.ensembleSize <= self.replicas'
              broker:
                description: broker configures the broker component.
                properties:
                  antiaffinity:
                    description: antiaffinity configures pod anti-affinity rules for
                      broker pods.
                    properties:
                      enabled:
                        default: true
                        description: enabled turns on the component's default anti-affinity
                          rules.
                        type: boolean
                      host:
                        default: false
                        description: |-
                          host requires (hard) rather than prefers (soft) spreading pods across nodes.
                          Stateful tiers default this to hard; stateless tiers default it to soft.
                        type: boolean
                      zone:
                        default: false
                        description: zone requires (hard) rather than prefers (soft)
                          spreading pods across zones.
                        type: boolean
                    type: object
                  autoscaler:
                    description: autoscaler configures CPU/throughput-driven horizontal
                      scaling.
                    properties:
                      enabled:
                        default: false
                        description: enabled turns on the broker autoscaler.
                        type: boolean
                      higherCpuThreshold:
                        default: 80
                        description: |-
                          higherCpuThreshold triggers a scale-up when every broker's load is at or
                          above it. Expressed as a whole-number percent in the range 0-100 (e.g. 80
                          means 80% CPU); autoscaler controllers divide by 100.
                        format: int32
                        maximum: 100
                        minimum: 0
                        type: integer
                      lowerCpuThreshold:
                        default: 30
                        description: |-
                          lowerCpuThreshold triggers a scale-down when every broker's load is below
                          it. Expressed as a whole-number percent in the range 0-100 (e.g. 30 means
                          30% CPU); autoscaler controllers divide by 100.
                        format: int32
                        maximum: 100
                        minimum: 0
                        type: integer
                      max:
                        description: max is the maximum number of brokers. The autoscaler
                          never scales above this.
                        format: int32
                        minimum: 1
                        type: integer
                      min:
                        description: min is the minimum number of brokers. The autoscaler
                          never scales below this.
                        format: int32
                        minimum: 1
                        type: integer
                      periodSeconds:
                        default: 60
                        description: periodSeconds is the interval between autoscaler
                          evaluations.
                        format: int32
                        minimum: 1
                        type: integer
                      resourcesUsageSource:
                        default: PulsarLBReport
                        description: |-
                          resourcesUsageSource selects where broker load is read from: the Pulsar load
                          manager report, or raw Kubernetes CPU metrics.
                        enum:
                        - PulsarLBReport
                        - K8SMetrics
                        type: string
                      scaleDownBy:
                        default: 1
                        description: scaleDownBy is the number of brokers to remove
                          on each scale-down.
                        format: int32
                        minimum: 1
                        type: integer
                      scaleUpBy:
                        default: 1
                        description: scaleUpBy is the number of brokers to add on
                          each scale-up.
                        format: int32
                        minimum: 1
                        type: integer
                      stabilizationWindowSeconds:
                        default: 300
                        description: |-
                          stabilizationWindowSeconds gates scaling decisions until all broker pods have
                          been continuously Ready for this long, preventing flapping.
                        format: int32
                        minimum: 0
                        type: integer
                    type: object
                    x-kubernetes-validations:
                    - message: lower threshold must be below higher
                      rule: self.lowerCpuThreshold < self.higherCpuThreshold
                    - message: min must be <= max
                      rule: '!has(self.max) || !has(self.min) || self.min <= self.max'
                  config:
                    additionalProperties:
                      type: string
                    description: config sets broker.conf key/value overrides layered
                      on top of operator defaults.
                    type: object
                  image:
                    description: |-
                      image overrides the broker container image. Falls back to
                      PulsarCluster.spec.image when unset.
                    type: string
                  loadBalancer:
                    default: extensible
                    description: |-
                      loadBalancer selects the Pulsar load manager implementation. "extensible"
                      (ExtensibleLoadManagerImpl) is required for live bundle-transfer scale-down
                      and is strongly recommended over the legacy "simple" load manager.
                    enum:
                    - extensible
                    - simple
                    type: string
                  pdb:
                    description: pdb configures the PodDisruptionBudget for broker
                      pods.
                    properties:
                      enabled:
                        default: true
                        description: enabled controls whether the operator creates
                          a PodDisruptionBudget for this component.
                        type: boolean
                      maxUnavailable:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          maxUnavailable is the maximum number of pods that can be unavailable during a
                          voluntary disruption. Defaults are computed from component quorum math.
                        x-kubernetes-int-or-string: true
                    type: object
                  replicas:
                    description: replicas is the number of broker pods.
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    description: resources are the compute resource requirements for
                      the broker container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              functionsWorker:
                description: functionsWorker configures the functions-worker component.
                properties:
                  config:
                    additionalProperties:
                      type: string
                    description: |-
                      config sets functions_worker.yml key/value overrides layered on top of
                      operator defaults.
                    type: object
                  image:
                    description: |-
                      image overrides the functions-worker container image. Falls back to
                      PulsarCluster.spec.image when unset.
                    type: string
                  mode:
                    default: colocated
                    description: |-
                      mode selects whether the functions worker runs colocated inside broker
                      pods or as its own standalone deployment.
                    enum:
                    - colocated
                    - standalone
                    type: string
                  packageStorage:
                    default: FileSystemPackagesStorage
                    description: |-
                      packageStorage selects the function-package storage backend. Oxia does
                      not provide the ZooKeeper-backed distributed log package storage KAAP
                      defaults to, so "FileSystemPackagesStorage" is required while the
                      metadata store is Oxia-only. There is no CEL rule for this: the enum
                      below has no ZooKeeper-dependent value to reject, since this operator's
                      metadataStore is Oxia-only and ZooKeeper package storage was never
                      exposed. If a ZooKeeper-backed value is ever added to the enum, add a
                      same-struct CEL rule rejecting it here.
                    enum:
                    - FileSystemPackagesStorage
                    - S3PackagesStorage
                    - GCSPackagesStorage
                    type: string
                  replicas:
                    description: |-
                      replicas is the number of standalone functions-worker pods. Ignored when
                      mode is "colocated".
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              global:
                description: global holds defaults applied across every component.
                properties:
                  affinity:
                    description: |-
                      affinity is the default pod affinity/anti-affinity applied to every
                      component unless overridden per-component.
                    properties:
                      nodeAffinity:
                        description: Describes node affinity scheduling rules for
                          the pod.
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and adding
                              "weight" to the sum if the node matches the corresponding matchExpressions; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: |-
                                An empty preferred scheduling term matches all objects with implicit weight 0
                                (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
                              properties:
                                preference:
                                  description: A node selector term, associated with
                                    the corresponding weight.
                                  properties:
                                    matchExpressions:
                                      description: A list of node selector requirements
                                        by node's labels.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchFields:
                                      description: A list of node selector requirements
                                        by node's fields.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  type: object
                                  x-kubernetes-map-type: atomic
                                weight:
                                  description: Weight associated with matching the
                                    corresponding nodeSelectorTerm, in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - preference
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to an update), the system
                              may or may not try to eventually evict the pod from its node.
                            properties:
                              nodeSelectorTerms:
                                description: Required. A list of node selector terms.
                                  The terms are ORed.
                                items:
                                  description: |-
                                    A null or empty node selector term matches no objects. The requirements of
                                    them are ANDed.
                                    The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                                  properties:
                                    matchExpressions:
                                      description: A list of node selector requirements
                                        by node's labels.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchFields:
                                      description: A list of node selector requirements
                                        by node's fields.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  type: object
                                  x-kubernetes-map-type: atomic
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - nodeSelectorTerms
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      podAffinity:
                        description: Describes pod affinity scheduling rules (e.g.
                          co-locate this pod in the same node, zone, etc. as some
                          other pod(s)).
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and adding
                              "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: The weights of all of the matched WeightedPodAffinityTerm
                                fields are added per-node to find the most preferred
                                node(s)
                              properties:
                                podAffinityTerm:
                                  description: Required. A pod affinity term, associated
                                    with the corresponding weight.
                                  properties:
                                    labelSelector:
                                      description: |-
                                        A label query over a set of resources, in this case pods.
                                        If it's null, this PodAffinityTerm matches with no Pods.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    matchLabelKeys:
                                      description: |-
                                        MatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                        Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    mismatchLabelKeys:
                                      description: |-
                                        MismatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                        Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    namespaceSelector:
                                      description: |-
                                        A label query over the set of namespaces that the term applies to.
                                        The term is applied to the union of the namespaces selected by this field
                                        and the ones listed in the namespaces field.
                                        null selector and null or empty namespaces list means "this pod's namespace".
                                        An empty selector ({}) matches all namespaces.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    namespaces:
                                      description: |-
                                        namespaces specifies a static list of namespace names that the term applies to.
                                        The term is applied to the union of the namespaces listed in this field
                                        and the ones selected by namespaceSelector.
                                        null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    topologyKey:
                                      description: |-
                                        This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                        the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                        whose value of the label with key topologyKey matches that of any node on which any of the
                                        selected pods is running.
                                        Empty topologyKey is not allowed.
                                      type: string
                                  required:
                                  - topologyKey
                                  type: object
                                weight:
                                  description: |-
                                    weight associated with matching the corresponding podAffinityTerm,
                                    in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - podAffinityTerm
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to a pod label update), the
                              system may or may not try to eventually evict the pod from its node.
                              When there are multiple elements, the lists of nodes corresponding to each
                              podAffinityTerm are intersected, i.e. all terms must be satisfied.
                            items:
                              description: |-
                                Defines a set of pods (namely those matching the labelSelector
                                relative to the given namespace(s)) that this pod should be
                                co-located (affinity) or not co-located (anti-affinity) with,
                                where co-located is defined as running on a node whose value of
                                the label with key <topologyKey> matches that of any node on which
                                a pod of the set of pods is running
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                      podAntiAffinity:
                        description: Describes pod anti-affinity scheduling rules
                          (e.g. avoid putting this pod in the same node, zone, etc.
                          as some other pod(s)).
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the anti-affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling anti-affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and subtracting
                              "weight" from the sum if the node has pods which matches the corresponding podAffinityTerm; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: The weights of all of the matched WeightedPodAffinityTerm
                                fields are added per-node to find the most preferred
                                node(s)
                              properties:
                                podAffinityTerm:
                                  description: Required. A pod affinity term, associated
                                    with the corresponding weight.
                                  properties:
                                    labelSelector:
                                      description: |-
                                        A label query over a set of resources, in this case pods.
                                        If it's null, this PodAffinityTerm matches with no Pods.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    matchLabelKeys:
                                      description: |-
                                        MatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                        Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    mismatchLabelKeys:
                                      description: |-
                                        MismatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                        Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    namespaceSelector:
                                      description: |-
                                        A label query over the set of namespaces that the term applies to.
                                        The term is applied to the union of the namespaces selected by this field
                                        and the ones listed in the namespaces field.
                                        null selector and null or empty namespaces list means "this pod's namespace".
                                        An empty selector ({}) matches all namespaces.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    namespaces:
                                      description: |-
                                        namespaces specifies a static list of namespace names that the term applies to.
                                        The term is applied to the union of the namespaces listed in this field
                                        and the ones selected by namespaceSelector.
                                        null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    topologyKey:
                                      description: |-
                                        This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                        the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                        whose value of the label with key topologyKey matches that of any node on which any of the
                                        selected pods is running.
                                        Empty topologyKey is not allowed.
                                      type: string
                                  required:
                                  - topologyKey
                                  type: object
                                weight:
                                  description: |-
                                    weight associated with matching the corresponding podAffinityTerm,
                                    in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - podAffinityTerm
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the anti-affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the anti-affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to a pod label update), the
                              system may or may not try to eventually evict the pod from its node.
                              When there are multiple elements, the lists of nodes corresponding to each
                              podAffinityTerm are intersected, i.e. all terms must be satisfied.
                            items:
                              description: |-
                                Defines a set of pods (namely those matching the labelSelector
                                relative to the given namespace(s)) that this pod should be
                                co-located (affinity) or not co-located (anti-affinity) with,
                                where co-located is defined as running on a node whose value of
                                the label with key <topologyKey> matches that of any node on which
                                a pod of the set of pods is running
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                    type: object
                  storageClassName:
                    description: |-
                      storageClassName is the default StorageClass for every component's PVCs,
                      unless overridden per-component.
                    type: string
                  zoneSpread:
                    default: true
                    description: |-
                      zoneSpread turns on a default topologySpreadConstraints (maxSkew=1) across
                      zones for every component, so multi-AZ HA works out of the box.
                    type: boolean
                type: object
              image:
                description: |-
                  image is the default Pulsar container image for every component, unless
                  overridden per-component.
                type: string
              imagePullSecrets:
                description: |-
                  imagePullSecrets are the default image pull secrets for every component's
                  pods.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              metadataStore:
                description: metadataStore selects and configures the cluster metadata
                  store.
                properties:
                  type:
                    default: oxia
                    description: type selects the metadata store implementation.
                    enum:
                    - oxia
                    type: string
                type: object
              offload:
                description: offload configures the cluster-default tiered-storage
                  offload policy.
                properties:
                  bucket:
                    description: |-
                      bucket is the object-storage bucket/container to offload into. Not used
                      for the filesystem driver.
                    type: string
                  credentialsSecretRef:
                    description: |-
                      credentialsSecretRef references the Secret holding the offload driver's
                      object-storage credentials.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  driver:
                    description: driver selects the tiered-storage backend.
                    enum:
                    - aws-s3
                    - google-cloud-storage
                    - azureblob
                    - filesystem
                    type: string
                  endpoint:
                    description: |-
                      endpoint overrides the object-storage service endpoint, for
                      S3-compatible stores that aren't AWS.
                    type: string
                  offloadThresholdBytes:
                    description: |-
                      offloadThresholdBytes is the managed-ledger size, in bytes, above which
                      closed ledgers are offloaded.
                    format: int64
                    type: integer
                  region:
                    description: region is the object-storage region. Not used for
                      the filesystem driver.
                    type: string
                required:
                - driver
                type: object
                x-kubernetes-validations:
                - message: bucket is required for object-store offload drivers (aws-s3,
                    google-cloud-storage, azureblob)
                  rule: self.driver == 'filesystem' || size(self.bucket) > 0
              oxia:
                description: oxia configures the metadata.OxiaCluster child resource.
                properties:
                  allowExtraAuthorities:
                    default: false
                    description: |-
                      allowExtraAuthorities allows oxia:// clients to specify additional shard
                      authorities beyond the coordinator's own service address.
                    type: boolean
                  coordinator:
                    description: coordinator configures the coordinator Deployment.
                    properties:
                      image:
                        description: |-
                          image overrides the coordinator container image. Falls back to
                          PulsarCluster.spec.image when unset.
                        type: string
                      replicas:
                        default: 2
                        description: |-
                          replicas is the number of coordinator pods. At least 2 are required for
                          coordinator leader-election HA.
                        format: int32
                        minimum: 1
                        type: integer
                      resources:
                        description: resources are the compute resource requirements
                          for the coordinator container.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                    type: object
                  namespaces:
                    description: namespaces are the Oxia namespaces to provision.
                    items:
                      description: |-
                        OxiaNamespaceSpec declares an Oxia namespace (a shard-keyspace) to
                        provision, one each for Pulsar's default/broker/bookkeeper metadata trees.
                      properties:
                        initialShardCount:
                          default: 3
                          description: initialShardCount is the number of shards the
                            namespace starts with.
                          format: int32
                          minimum: 1
                          type: integer
                        name:
                          description: name is the Oxia namespace name.
                          minLength: 1
                          type: string
                        replicationFactor:
                          default: 3
                          description: |-
                            replicationFactor is the number of server replicas each shard is
                            replicated to.
                          format: int32
                          minimum: 1
                          type: integer
                      required:
                      - name
                      type: object
                    type: array
                  server:
                    description: server configures the server StatefulSet.
                    properties:
                      dbCacheSizeMb:
                        description: dbCacheSizeMb sizes the server's RocksDB block
                          cache, in megabytes.
                        format: int32
                        type: integer
                      image:
                        description: |-
                          image overrides the server container image. Falls back to
                          PulsarCluster.spec.image when unset.
                        type: string
                      replicas:
                        default: 3
                        description: replicas is the number of server pods. Must be
                          odd for quorum correctness.
                        format: int32
                        minimum: 1
                        type: integer
                      storageClassName:
                        description: |-
                          storageClassName is the StorageClass used to provision each server pod's
                          data/wal PVCs. Falls back to PulsarCluster.spec.global.storageClassName
                          when unset.
                        type: string
                      storageSize:
                        anyOf:
                        - type: integer
                        - type: string
                        description: storageSize is the requested capacity for each
                          server pod's data/wal PVCs.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      walSyncData:
                        default: true
                        description: |-
                          walSyncData forces fsync on every WAL write for durability at the cost of
                          write latency.
                        type: boolean
                    type: object
                    x-kubernetes-validations:
                    - message: oxia server replicas must be odd (quorum)
                      rule: self.replicas % 2 == 1
                type: object
              proxy:
                description: proxy configures the optional proxy component.
                properties:
                  autoscaler:
                    description: autoscaler optionally configures CPU-based horizontal
                      scaling.
                    properties:
                      enabled:
                        default: false
                        description: enabled turns on the proxy autoscaler.
                        type: boolean
                      max:
                        description: max is the maximum number of proxy pods.
                        format: int32
                        minimum: 1
                        type: integer
                      min:
                        description: min is the minimum number of proxy pods.
                        format: int32
                        minimum: 1
                        type: integer
                      targetCPUUtilizationPercentage:
                        default: 80
                        description: |-
                          targetCPUUtilizationPercentage is the average CPU utilization target across
                          proxy pods.
                        format: int32
                        maximum: 100
                        minimum: 1
                        type: integer
                    type: object
                  config:
                    additionalProperties:
                      type: string
                    description: config sets proxy.conf key/value overrides layered
                      on top of operator defaults.
                    type: object
                  image:
                    description: |-
                      image overrides the proxy container image. Falls back to
                      PulsarCluster.spec.image when unset.
                    type: string
                  replicas:
                    description: replicas is the number of proxy pods.
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    description: resources are the compute resource requirements for
                      the proxy container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  tls:
                    description: tls configures TLS/mTLS termination on the proxy.
                    properties:
                      enabled:
                        default: false
                        description: enabled turns on TLS termination on the proxy's
                          client-facing ports.
                        type: boolean
                      secretName:
                        description: |-
                          secretName is the Secret holding the TLS certificate/key served by the
                          proxy. Required when enabled is true.
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: tls.secretName is required when tls.enabled is true
                      rule: '!self.enabled || (has(self.secretName) && size(self.secretName)
                        > 0)'
                type: object
              pulsarVersion:
                default: 5.0.0-M1
                description: pulsarVersion pins the exact Pulsar image tag deployed
                  across components.
                type: string
            type: object
          status:
            description: status defines the observed state of PulsarCluster
            properties:
              autoRecoveryPhase:
                description: autoRecoveryPhase is the observed phase of the child
                  AutoRecovery resource.
                type: string
              bookKeeperPhase:
                description: bookKeeperPhase is the observed phase of the child BookKeeper
                  resource.
                type: string
              brokerPhase:
                description: brokerPhase is the observed phase of the child Broker
                  resource.
                type: string
              conditions:
                description: conditions represent the current state of the PulsarCluster
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              functionsWorkerPhase:
                description: functionsWorkerPhase is the observed phase of the child
                  FunctionsWorker resource.
                type: string
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              oxiaPhase:
                description: oxiaPhase is the observed phase of the child OxiaCluster
                  resource.
                type: string
              proxyPhase:
                description: proxyPhase is the observed phase of the child Proxy resource.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
