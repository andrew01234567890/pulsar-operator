---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: oxiaclusters.metadata.pulsaroperator.io
spec:
  group: metadata.pulsaroperator.io
  names:
    kind: OxiaCluster
    listKind: OxiaClusterList
    plural: oxiaclusters
    singular: oxiacluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.coordinatorReplicas
      name: Coordinators
      type: integer
    - jsonPath: .status.serverReplicas
      name: Servers
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OxiaCluster is the Schema for the oxiaclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OxiaCluster
            properties:
              allowExtraAuthorities:
                default: false
                description: |-
                  allowExtraAuthorities allows oxia:// clients to specify additional shard
                  authorities beyond the coordinator's own service address.
                type: boolean
              coordinator:
                description: coordinator configures the coordinator Deployment.
                properties:
                  image:
                    description: |-
                      image overrides the coordinator container image. Falls back to
                      PulsarCluster.spec.image when unset.
                    type: string
                  replicas:
                    default: 2
                    description: |-
                      replicas is the number of coordinator pods. At least 2 are required for
                      coordinator leader-election HA.
                    format: int32
                    minimum: 1
                    type: integer
                  resources:
                    description: resources are the compute resource requirements for
                      the coordinator container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              namespaces:
                description: namespaces are the Oxia namespaces to provision.
                items:
                  description: |-
                    OxiaNamespaceSpec declares an Oxia namespace (a shard-keyspace) to
                    provision, one each for Pulsar's default/broker/bookkeeper metadata trees.
                  properties:
                    initialShardCount:
                      default: 3
                      description: initialShardCount is the number of shards the namespace
                        starts with.
                      format: int32
                      minimum: 1
                      type: integer
                    name:
                      description: name is the Oxia namespace name.
                      minLength: 1
                      type: string
                    replicationFactor:
                      default: 3
                      description: |-
                        replicationFactor is the number of server replicas each shard is
                        replicated to.
                      format: int32
                      minimum: 1
                      type: integer
                  required:
                  - name
                  type: object
                type: array
              server:
                description: server configures the server StatefulSet.
                properties:
                  dbCacheSizeMb:
                    description: dbCacheSizeMb sizes the server's RocksDB block cache,
                      in megabytes.
                    format: int32
                    type: integer
                  image:
                    description: |-
                      image overrides the server container image. Falls back to
                      PulsarCluster.spec.image when unset.
                    type: string
                  replicas:
                    default: 3
                    description: |-
                      replicas is the number of server pods. Must be odd for quorum correctness;
                      oddness is enforced by a future validating webhook.
                    format: int32
                    minimum: 1
                    type: integer
                  storageClassName:
                    description: |-
                      storageClassName is the StorageClass used to provision each server pod's
                      data/wal PVCs. Falls back to PulsarCluster.spec.global.storageClassName
                      when unset.
                    type: string
                  storageSize:
                    anyOf:
                    - type: integer
                    - type: string
                    description: storageSize is the requested capacity for each server
                      pod's data/wal PVCs.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  walSyncData:
                    default: true
                    description: |-
                      walSyncData forces fsync on every WAL write for durability at the cost of
                      write latency.
                    type: boolean
                type: object
            type: object
          status:
            description: status defines the observed state of OxiaCluster
            properties:
              conditions:
                description: conditions represent the current state of the OxiaCluster
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              coordinatorReplicas:
                description: coordinatorReplicas is the observed number of Ready coordinator
                  pods.
                format: int32
                type: integer
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              serverReplicas:
                description: serverReplicas is the observed number of Ready server
                  pods.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
